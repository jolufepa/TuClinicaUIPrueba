<mah:MetroWindow x:Class="TuClinica.UI.Views.OdontogramWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:local="clr-namespace:TuClinica.UI.Views"
                 xmlns:vm="clr-namespace:TuClinica.UI.ViewModels"
                 xmlns:ctrl="clr-namespace:TuClinica.UI.Views.Controls"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 mc:Ignorable="d"
                 Title="Odontograma"
                 Height="700" Width="1280"
                 WindowStartupLocation="CenterScreen"
                 WindowState="Maximized"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                 d:DataContext="{d:DesignInstance Type=vm:OdontogramViewModel}">

    <Window.Resources>
        <Style x:Key="ToolButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource MahApps.Styles.ToggleButton.Flat}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#EEEEEE" BorderThickness="0,0,1,0" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Width="100">
                    <TextBlock Text="HERRAMIENTAS" FontWeight="Bold" Foreground="Gray" Margin="0,0,0,10" HorizontalAlignment="Center" FontSize="10"/>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Cursor" IsChecked="True" ToolTip="Modo Selección">
                        <StackPanel>
                            <iconPacks:PackIconModern Kind="CursorDefault" Width="24" Height="24" HorizontalAlignment="Center"/>
                            <TextBlock Text="Cursor" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Caries">
                        <StackPanel>
                            <Rectangle Width="20" Height="20" Fill="#E74C3C" RadiusX="2" RadiusY="2" HorizontalAlignment="Center"/>
                            <TextBlock Text="Caries" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Obturacion">
                        <StackPanel>
                            <Rectangle Width="20" Height="20" Fill="#3498DB" RadiusX="2" RadiusY="2" HorizontalAlignment="Center"/>
                            <TextBlock Text="Obturación" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Corona">
                        <StackPanel>
                            <Ellipse Width="20" Height="20" Fill="#F1C40F" HorizontalAlignment="Center"/>
                            <TextBlock Text="Corona" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Endodoncia">
                        <StackPanel>
                            <Rectangle Width="6" Height="20" Fill="#9B59B6" HorizontalAlignment="Center"/>
                            <TextBlock Text="Endodoncia" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Implante">
                        <StackPanel>
                            <iconPacks:PackIconModern Kind="HardwareCpu" Width="20" Height="20" Foreground="Gray" HorizontalAlignment="Center"/>
                            <TextBlock Text="Implante" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <Separator Margin="0,10"/>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Ausente">
                        <StackPanel>
                            <Ellipse Width="20" Height="20" Fill="Gray" Opacity="0.5" HorizontalAlignment="Center"/>
                            <TextBlock Text="Ausente" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Extraccion">
                        <StackPanel>
                            <iconPacks:PackIconModern Kind="Close" Width="20" Height="20" Foreground="Red" HorizontalAlignment="Center"/>
                            <TextBlock Text="Extracción" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource ToolButtonStyle}" Command="{Binding SelectToolCommand}" CommandParameter="Borrador">
                        <StackPanel>
                            <iconPacks:PackIconMaterial Kind="Eraser" Width="20" Height="20" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="Borrador" FontSize="11"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Patient.FullName, StringFormat='Paciente: {0}'}" 
                       FontSize="20" FontWeight="Light" Margin="20,15,0,0" Foreground="#333"/>

            <Viewbox Grid.Row="1" Stretch="Uniform" Margin="20">
                <Grid Width="300" Height="350">
                    <ItemsControl ItemsSource="{Binding Odontogram}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ctrl:ToothControl />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Viewbox>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
                <Button Content="Imprimir PDF" Command="{Binding PrintCommand}" Margin="0,0,20,0"/>
                <Button Content="Guardar Cambios" Command="{Binding AcceptCommand}" Style="{StaticResource MahApps.Styles.Button.Dialogs.Accent}" Margin="0,0,10,0"/>
                <Button Content="Cancelar" Command="{Binding CancelCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Column="2" Background="White" BorderBrush="#EEEEEE" BorderThickness="1,0,0,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Resumen Estado" FontWeight="Bold" FontSize="14" Margin="0,10,0,15" HorizontalAlignment="Center"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding SummaryList}" AutoGenerateColumns="False" IsReadOnly="True" 
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0" Background="White">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="" Width="30">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Ellipse Width="12" Height="12" Fill="{Binding ColorIndicator}" HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Diente" Binding="{Binding ToothNumber}" FontWeight="Bold" Width="50"/>
                        <DataGridTextColumn Header="Detalle" Binding="{Binding Description}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</mah:MetroWindow>
<UserControl x:Class="TuClinica.UI.Views.Controls.ToothControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:TuClinica.UI.ViewModels"
             xmlns:converters="clr-namespace:TuClinica.UI.Converters" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             d:DataContext="{d:DesignInstance Type=viewmodels:ToothViewModel}"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TuClinica.UI;component/Assets/ToothGeometries.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <converters:ToothNumberToGeometryConverter x:Key="ToothToGeoConverter"/>
            <converters:ToothNumberToTextGeometryConverter x:Key="ToothToTextConverter"/>
            <converters:SurfaceColorConverter x:Key="SurfaceColorConverter"/>

            <DrawingBrush x:Key="AbsentPattern" TileMode="Tile" Viewport="0,0,8,8" ViewportUnits="Absolute">
                <DrawingBrush.Drawing>
                    <GeometryDrawing>
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <LineGeometry StartPoint="0,0" EndPoint="8,8" />
                                <LineGeometry StartPoint="0,8" EndPoint="8,0" />
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                        <GeometryDrawing.Pen>
                            <Pen Brush="#C0392B" Thickness="1" />
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                </DrawingBrush.Drawing>
            </DrawingBrush>

            <Style x:Key="SurfacePathStyle" TargetType="Path">
                <Setter Property="Stroke" Value="#333333"/>
                <Setter Property="StrokeThickness" Value="0.5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="IsHitTestVisible" Value="True"/>

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.8"/>
                        <Setter Property="Panel.ZIndex" Value="100"/>
                    </Trigger>

                    <DataTrigger Binding="{Binding FullCondition}" Value="Ausente">
                        <Setter Property="Fill" Value="{StaticResource AbsentPattern}"/>
                        <Setter Property="Stroke" Value="#C0392B"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid ClipToBounds="False" VerticalAlignment="Top" HorizontalAlignment="Left">

        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseRightButtonDown">
                <i:InvokeCommandAction Command="{Binding SurfaceClickCommand}" CommandParameter="Completo"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>

        <Grid>
            <Path Data="{Binding ToothNumber, Converter={StaticResource ToothToGeoConverter}, ConverterParameter='Vestibular'}" ToolTip="Vestibular">
                <Path.Style>
                    <Style TargetType="Path" BasedOn="{StaticResource SurfacePathStyle}">
                        <Setter Property="Fill">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource SurfaceColorConverter}">
                                    <Binding Path="VestibularCondition" />
                                    <Binding Path="VestibularRestoration" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Path.Style>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding SurfaceClickCommand}" CommandParameter="Vestibular"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Path>

            <Path Data="{Binding ToothNumber, Converter={StaticResource ToothToGeoConverter}, ConverterParameter='Lingual'}" ToolTip="Lingual">
                <Path.Style>
                    <Style TargetType="Path" BasedOn="{StaticResource SurfacePathStyle}">
                        <Setter Property="Fill">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource SurfaceColorConverter}">
                                    <Binding Path="LingualCondition" />
                                    <Binding Path="LingualRestoration" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Path.Style>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding SurfaceClickCommand}" CommandParameter="Lingual"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Path>

            <Path Data="{Binding ToothNumber, Converter={StaticResource ToothToGeoConverter}, ConverterParameter='Mesial'}" ToolTip="Mesial">
                <Path.Style>
                    <Style TargetType="Path" BasedOn="{StaticResource SurfacePathStyle}">
                        <Setter Property="Fill">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource SurfaceColorConverter}">
                                    <Binding Path="MesialCondition" />
                                    <Binding Path="MesialRestoration" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Path.Style>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding SurfaceClickCommand}" CommandParameter="Mesial"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Path>

            <Path Data="{Binding ToothNumber, Converter={StaticResource ToothToGeoConverter}, ConverterParameter='Distal'}" ToolTip="Distal">
                <Path.Style>
                    <Style TargetType="Path" BasedOn="{StaticResource SurfacePathStyle}">
                        <Setter Property="Fill">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource SurfaceColorConverter}">
                                    <Binding Path="DistalCondition" />
                                    <Binding Path="DistalRestoration" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Path.Style>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding SurfaceClickCommand}" CommandParameter="Distal"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Path>

            <Path Data="{Binding ToothNumber, Converter={StaticResource ToothToGeoConverter}, ConverterParameter='Oclusal'}" ToolTip="Oclusal">
                <Path.Style>
                    <Style TargetType="Path" BasedOn="{StaticResource SurfacePathStyle}">
                        <Setter Property="Fill">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource SurfaceColorConverter}">
                                    <Binding Path="OclusalCondition" />
                                    <Binding Path="OclusalRestoration" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Path.Style>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding SurfaceClickCommand}" CommandParameter="Oclusal"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Path>
        </Grid>

        <Path Data="{Binding ToothNumber, Converter={StaticResource ToothToTextConverter}}"
              Fill="#34495E"
              IsHitTestVisible="False"
              Panel.ZIndex="1000"
              RenderTransformOrigin="0.5,0.5">
            <Path.RenderTransform>
                <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
            </Path.RenderTransform>
        </Path>

    </Grid>
</UserControl>
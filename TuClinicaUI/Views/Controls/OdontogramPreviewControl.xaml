<UserControl x:Class="TuClinica.UI.Views.Controls.OdontogramPreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:ctrl="clr-namespace:TuClinica.UI.Views.Controls"
             Height="Auto">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Odontograma" 
                       FontWeight="SemiBold" 
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <Button Content="Abrir completo" 
                    Command="{Binding OpenFullOdontogramCommand}"
                    HorizontalAlignment="Right"
                    Style="{StaticResource MahApps.Styles.Button.Dialogs.Accent}"
                    Padding="10,5"/>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ItemsControl Grid.Row="0" ItemsSource="{Binding UpperArch}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="16" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Viewbox Stretch="Uniform" Margin="1">
                            <ctrl:ToothControl DataContext="{Binding}" Width="75" Height="75" />
                        </Viewbox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl Grid.Row="1" ItemsSource="{Binding LowerArch}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="16" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Viewbox Stretch="Uniform" Margin="1">
                            <ctrl:ToothControl DataContext="{Binding}" Width="75" Height="75" />
                        </Viewbox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Grid>

        <StackPanel Grid.Row="2" Margin="0,15,0,0" HorizontalAlignment="Center">
            <TextBlock Text="Leyenda de Colores:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5" HorizontalAlignment="Center"/>

            <Grid TextBlock.FontSize="11">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Width="14" Height="14" Background="White" BorderBrush="Black" BorderThickness="1" Margin="0,0,5,5"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Sano" VerticalAlignment="Center" Margin="0,0,10,5"/>

                <Border Grid.Row="0" Grid.Column="2" Width="14" Height="14" Background="Red" Margin="0,0,5,5"/>
                <TextBlock Grid.Row="0" Grid.Column="3" Text="Caries" VerticalAlignment="Center" Margin="0,0,10,5"/>

                <Border Grid.Row="0" Grid.Column="4" Width="14" Height="14" Background="Black" Margin="0,0,5,5"/>
                <TextBlock Grid.Row="0" Grid.Column="5" Text="Ausente" VerticalAlignment="Center" Margin="0,0,10,5"/>

                <Border Grid.Row="1" Grid.Column="0" Width="14" Height="14" Background="Blue" Margin="0,0,5,5"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Restauración" VerticalAlignment="Center" Margin="0,0,10,5"/>

                <Border Grid.Row="1" Grid.Column="2" Width="14" Height="14" Background="Yellow" Margin="0,0,5,5"/>
                <TextBlock Grid.Row="1" Grid.Column="3" Text="Planificado" VerticalAlignment="Center" Margin="0,0,10,5"/>
            </Grid>
        </StackPanel>

    </Grid>
</UserControl>
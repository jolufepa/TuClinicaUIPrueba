<UserControl x:Class="TuClinica.UI.Views.FinancialSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:viewmodels="clr-namespace:TuClinica.UI.ViewModels"
             xmlns:converters="clr-namespace:TuClinica.UI.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:FinancialSummaryViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <ResourceDictionary>
            <converters:CurrencyDashConverter x:Key="CurrencyDashConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Resumen Financiero (Libro Diario)" FontSize="20" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <Label Content="Desde:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding StartDate}" Margin="5,0,10,0" Width="120"/>

                <Label Content="Hasta:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding EndDate}" Margin="5,0,10,0" Width="120"/>

                <Button Content="Buscar" Command="{Binding SearchCommand}" Style="{StaticResource MahApps.Styles.Button.Square.Accent}" Margin="5,0"/>
                <Button Content="Hoy" Command="{Binding SetTodayCommand}" Margin="5,0"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Imprimir PDF" Command="{Binding PrintReportCommand}" HorizontalAlignment="Right">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Kind="AdobeAcrobat" Margin="0,0,5,0"/>
                            <TextBlock Text="Imprimir"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" 
                  IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal" 
                  BorderThickness="1" BorderBrush="#DDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Date, StringFormat='dd/MM/yyyy HH:mm'}" Width="Auto"/>
                <DataGridTextColumn Header="Paciente" Binding="{Binding PatientName}" Width="2*"/>
                <DataGridTextColumn Header="Detalle" Binding="{Binding Description}" Width="3*"/>

                <DataGridTextColumn Header="Cargo" Binding="{Binding ChargeAmount, Converter={StaticResource CurrencyDashConverter}}" Width="*" FontWeight="SemiBold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#D32F2F"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Abono" Binding="{Binding PaymentAmount, Converter={StaticResource CurrencyDashConverter}}" Width="*" FontWeight="SemiBold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#388E3C"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1" Margin="0,15,0,0" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="TOTAL PRODUCCIÓN (Cargos)" Foreground="Gray" FontSize="12"/>
                    <TextBlock Text="{Binding TotalCharges, StringFormat=C, ConverterCulture='es-ES'}" FontSize="20" FontWeight="Bold" Foreground="#D32F2F"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="TOTAL CAJA (Cobrado)" Foreground="Gray" FontSize="12"/>
                    <TextBlock Text="{Binding TotalPayments, StringFormat=C, ConverterCulture='es-ES'}" FontSize="24" FontWeight="Bold" Foreground="#388E3C"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="BALANCE PERIODO" Foreground="Gray" FontSize="12"/>
                    <TextBlock Text="{Binding NetBalance, StringFormat=C, ConverterCulture='es-ES'}" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
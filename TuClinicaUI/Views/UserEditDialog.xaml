<Window x:Class="TuClinica.UI.Views.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TuClinica.UI.Views"
        xmlns:viewmodels="clr-namespace:TuClinica.UI.ViewModels" xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        
        d:DataContext="{d:DesignInstance Type=viewmodels:UserEditViewModel}" 

        Title="{Binding Title}" Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="_Usuario:" Target="{Binding ElementName=UsernameTextBox}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="UsernameTextBox"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="5" mah:TextBoxHelper.ClearTextButton="True"/>
                 

        <Label Grid.Row="1" Grid.Column="0" Content="_Contraseña:" Target="{Binding ElementName=PasswordBox}" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PasswordBox"
                     mah:TextBoxHelper.Watermark="(Dejar en blanco para no cambiar)"
                     mah:TextBoxHelper.ClearTextButton="True"
                     Margin="5"/>
        <Label Grid.Row="2" Grid.Column="0" Content="_Rol:" Target="{Binding ElementName=RoleComboBox}" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="RoleComboBox"
                  ItemsSource="{Binding AvailableRoles}"
                  SelectedItem="{Binding SelectedRole}"
                  Margin="5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Nº Colegiado:" Target="{Binding ElementName=CollegeTextBox}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="CollegeTextBox"
             Text="{Binding CollegeNumber, UpdateSourceTrigger=PropertyChanged}" Margin="5" mah:TextBoxHelper.ClearTextButton="True"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Especialidad:" Target="{Binding ElementName=SpecialtyTextBox}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="SpecialtyTextBox"
             Text="{Binding Specialty, UpdateSourceTrigger=PropertyChanged}" Margin="5" mah:TextBoxHelper.ClearTextButton="True"/>

        <CheckBox Grid.Row="5" Grid.Column="1" Content="Usuario Activo"
              IsChecked="{Binding IsActive}"
              Margin="5,10,5,5"/>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Guardar"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding ElementName=PasswordBox}"
                    IsDefault="True"
                    Style="{StaticResource MahApps.Styles.Button.Dialogs.Accent}" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
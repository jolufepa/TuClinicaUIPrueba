namespace TuClinica.UI.Messages
{
    /// <summary>
    /// Mensaje para solicitar la apertura del odontograma completo (modal)
    /// </summary>
    public class OpenOdontogramMessage
    {
        // Este mensaje es vacío porque no necesita datos.
        // Solo sirve como señal para que PatientFileViewModel abra OdontogramWindow.
    }
}